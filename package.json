{
  "name": "corporate-landing-page",
  "version": "1.0.0",
  "description": "Performance-Optimized Landing Page",
  "main": "index.html",
  "scripts": {
    "build": "mkdir -p css && npx tailwindcss -i ./src/input.css -o ./css/output.css --minify",
    "watch": "npx tailwindcss -i ./src/input.css -o ./css/output.css --watch"
  },
  "dependencies": {
    "tailwindcss": "^3.4.1"
  }
}